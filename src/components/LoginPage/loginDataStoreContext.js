import React from "react";

const LoginDataStoreContext = React.createContext(undefined)

export const LoginingUserStoreProvider = LoginDataStoreContext.Provider;

export default LoginDataStoreContext;